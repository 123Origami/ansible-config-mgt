pipeline {
    agent any

    parameters {
        string(
            name: 'inventory',
<<<<<<< HEAD
            defaultValue: 'dev',
            description: 'This is the inventory file for the environment to deploy configuration'
=======
            choices: ['dev', 'sit', 'uat', 'pentest', 'pre-prod', 'prod'],
            description: 'Select environment to deploy to'
>>>>>>> 8b59d6d63d939c3997797bfcc612ea71c182cc75
        )
        string(
            name: 'tags',
            defaultValue: 'all',
            description: 'Ansible tags to run (comma separated)'
        )
    }

    environment {
        ANSIBLE_CONFIG = "${WORKSPACE}/deploy/.ansible.cfg"
        INVENTORY_FILE = "${params.inventory}"
    }

    stages {
        stage('Clean Workspace') {
            steps {
                cleanWs()
            }
        }

        stage('Checkout Code') {
            steps {
                git branch: 'main', 
                url: 'https://github.com/123Origami/ansible-config-mgt.git'
            }
        }

        stage('Setup Environment') {
            steps {
<<<<<<< HEAD
                script {
                    // Update ansible.cfg with dynamic roles_path
                    sh '''
                    echo "=== Setting up environment ==="
                    echo "Inventory: ${inventory}"
                    echo "Tags: ${tags}"
                    echo "ANSIBLE_CONFIG: ${ANSIBLE_CONFIG}"
                    
                    # Update ansible.cfg with current workspace
                    sed -i "s|\\\$WORKSPACE|${WORKSPACE}|g" deploy/.ansible.cfg
                    sed -i "s|\\\$INVENTORY_FILE|${inventory}|g" deploy/.ansible.cfg
                    
                    echo "=== Updated .ansible.cfg ==="
                    cat deploy/.ansible.cfg
                    '''
                }
=======
                echo "Starting deployment to ${params.inventory}"
                sh '''
                echo "Working directory: $(pwd)"
                echo "Inventory: ${inventory}"
                echo "Tags: ${tags}"
                echo ""
                echo "=== Checking files ==="
                ls -la
                '''
>>>>>>> 8b59d6d63d939c3997797bfcc612ea71c182cc75
            }
        }

        stage('Run Ansible Playbook') {
            steps {
                sh '''
<<<<<<< HEAD
                echo "=== Running Ansible Playbook ==="
                echo "Current directory: $(pwd)"
                echo "Inventory file: ${inventory}"
                
                # Test if inventory file exists
                if [ -f "${inventory}" ]; then
                    echo "Inventory file content:"
                    cat "${inventory}"
                else
                    echo "ERROR: Inventory file ${inventory} not found!"
                    exit 1
=======
                echo "=== Running Ansible ==="
                ansible --version
                echo ""
                echo "=== Testing connectivity ==="
                if [ -f "${inventory}" ]; then
                    ansible all -i ${inventory} -m ping -u ubuntu || echo "Ping test failed"
                else
                    echo "Inventory file ${inventory} not found"
>>>>>>> 8b59d6d63d939c3997797bfcc612ea71c182cc75
                fi
                
                # Run Ansible
                echo ""
                echo "Running ansible-playbook..."
                ansible-playbook -i ${inventory} site.yml --tags "${tags}" -v
                '''
            }
        }

        stage('Verify Deployment') {
            steps {
<<<<<<< HEAD
                echo "✅ Deployment to ${params.inventory} completed successfully!"
=======
                echo "Deployment to ${params.inventory} completed!"
>>>>>>> 8b59d6d63d939c3997797bfcc612ea71c182cc75
                sh '''
                echo "=== Verification ==="
                date
                echo "All tasks completed!"
                '''
            }
        }
    }

    post {
        always {
<<<<<<< HEAD
            echo "📊 Pipeline ${currentBuild.currentResult}"
            echo "Inventory: ${params.inventory}"
            echo "Tags: ${params.tags}"
=======
            echo "Pipeline ${currentBuild.currentResult}"
>>>>>>> 8b59d6d63d939c3997797bfcc612ea71c182cc75
        }
        success {
            echo "🎉 SUCCESS! Ansible deployment completed!"
        }
        failure {
            echo "❌ FAILURE! Check Ansible logs above."
        }
    }
}
